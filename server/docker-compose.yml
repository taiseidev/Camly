version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: camly-app
    volumes:
      - ./app:/app
    working_dir: /app
    ports:
      - "8080:8080"
    environment:
      - GO_ENV=development
    depends_on: []
